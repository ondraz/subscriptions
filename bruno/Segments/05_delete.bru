meta {
  name: Delete segment
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/segments/:segmentId
  body: none
  auth: bearer
}

params:path {
  segmentId: {{segmentId}}
}

auth:bearer {
  token: {{apiKey}}
}
